name: registry
description: Fast service locator for Dart and Flutter with support for deep injection params. No dependencies, no code generation.
homepage: https://github.com/adrianflutur/registry
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
